/// The Richman game engine: board, players, turns, cards, and economy.
/// No UI or networking dependencies — see `Docs/ARCHITECTURE.md`.
public enum RichmanCore {
    public static let moduleName = "RichmanCore"
}
